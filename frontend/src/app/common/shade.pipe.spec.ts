import { ShadePipe } from './shade.pipe';

describe('ShadePipe', () => {
	it('create an instance', () => {
		const pipe = new ShadePipe();
		expect(pipe).toBeTruthy();
	});

	/* it('make darken', () => {

	}); */
});
