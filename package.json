{
	"name": "pointing-blackjack",
	"version": "0.0.3",
	"description": "Pointing Poker with blackjack and hookers",
	"main": "dist/server.js",
	"dependencies": {
		"express": "^4.17.1",
		"lodash": "^4.17.15",
		"socket.io": "^2.3.0",
		"ws": "^7.2.3"
	},
	"devDependencies": {},
	"scripts": {
		"heroku-postbuild": "cd server && npx tsc && cd ../frontend && npx ng build --prod",
		"build-dev": "cd server && tsc && cd ../frontend && ng build",
		"build-prod": "cd server && tsc && cd ../frontend && ng build --prod",
		"test": "cd frontend && ng lint && ng test --watch=false --no-progress --browsers=ChromeHeadlessNoSandbox",
		"start": "node dist/server.js",
		"postinstall": "cd server && npm install && cd ../frontend && npm install"
	},
	"engines": {
		"node": "12.11.x"
	},
	"cacheDirectories": [
		"node_modules",
		"server/node_modules",
		"frontend/node_modules"
	],
	"author": "Yurius the Great",
	"license": "MIT"
}
